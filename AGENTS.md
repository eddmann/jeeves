TypeScript + Bun runtime.

Run `make help` for commands, `make can-release` to validate.

Detroit-style tests: assert on observable behaviour, real objects over mocks, dependency injection for LLM boundary.

See [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) for detailed system design.
